# 兼职人工费用与提成分页

## 页面

- 提成设置在页脚显示筛选后的商品总数、当前页和总页数。游标翻页不会改变总数的筛选范围。
- 左侧“兼职人工费用”进入 /labor。按月维护费项科目名称和总金额，展示各店销售收入、占比及分摊金额。
- 总额未录入保留空值。没有可分摊销售时保存总额但不伪造分摊；销售资料未齐时显示核对提示。
- 金额与名称保存到现有 overheads.csv，沿用已有公司公摊计算。修改保留审计记录，并检查并发版本；已有店铺结账的月份拒绝直接改总额。
- 页面不改已存提成记录。现有旧版公司提成汇总继续读取同一份公摊总额；本次没有对新版已存提成另加一次扣款。

## 验证

接口测试覆盖录入、修改、刷新读取、销售比例25%/75%、并发冲突、已结账拒绝修改、总页数及筛选边界。真实商品目录只读测量：全量126143件、2103页；拾梦小屋未设置761件、13页（目录随同步变化）。

## 生产恢复

D盘满时发布未完成。旧测试目录与三份历史备份归档到C盘并核验哈希，恢复发布空间。未删除业务原始表、账务运行记录或在用数据库。

拾梦小屋修复已发布为a4fba45；六月重算记录111061，支付宝11559行6.95全额计入，11328行14.04全额计入，4158行258挂回5122217701379005445。重算前后已结账记录一致。
