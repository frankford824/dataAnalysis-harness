# ledger

把各平台交上来的表算成账。

店长每天、每周或者任意时刻把导出的表交上来，引擎自己认出这是哪家店、哪个平台、
哪个账期、每张表是什么、该按谁的口径算，然后出一张损益表，并且在出数之前先说清
这个月能不能结账、缺什么。

规则不写在代码里，写在模型数据里。加一个平台、改一项利润口径，改的是 YAML，
不是 Python。

## 装

```bash
python -m venv .venv
.venv/bin/pip install -r requirements.txt
```

## 用

命令行，一个路径就够：

```bash
.venv/bin/python -m ledger.cli run /path/to/平台数据
.venv/bin/python -m ledger.cli run /path/to/平台数据 --store 淘宝喜必顺
.venv/bin/python -m ledger.cli run /path/to/平台数据 --json    # 给接口和界面用
.venv/bin/python -m ledger.cli stores                          # 看店铺注册表
```

网页版，店长用这个：

```bash
.venv/bin/python -m uvicorn ledger.api:app --port 8848
```

打开 <http://127.0.0.1:8848/>，把这个月的表拖进去。

**没有登录，也没有权限分级。** 谁能连上这个端口谁就能改账。现在靠局域网和防火墙
把范围圈住，别把它暴露到公网。因此从网页做的操作留不下操作人——界面上「交表人」
显示成破折号就是这个意思。需要签字的场合走命令行：`ledger submit --by 张三`。

**文件名不能改。** 认哪家店、认哪张表全靠它——交上来的文件名形如
`聚水潭成本-淘宝喜必顺.xlsx`，破折号前是类别、后面是店铺。改了名字，引擎认不出
归属，那批数据不会进账（会明确列出来，不会悄悄算漏）。

## 模型

一个模型是一个目录，`models/cn-ecommerce` 是国内电商这一套：

| 文件 | 是什么 |
| --- | --- |
| `stores.yaml` | 店铺、平台、法人主体、归档状态 |
| `sources.yaml` | 每类数据由谁交、多久交一次、结账是否必须 |
| `templates.yaml` | 表头长什么样、哪列是什么、怎么取订单号、怎么归科目 |
| `metrics.yaml` | 每个口径项怎么算、挂到哪、怎么分摊 |
| `statement.yaml` | 损益表的公式树 |
| `checks.yaml` | 结账前的拦截条件 |
| `dictionary.csv` | 平台原始科目到统一科目的映射 |

各平台的利润口径本来就不一样，模型如实表达而不是强行统一：淘宝七项费用按销售收入
占比分摊到子订单，1688 按笔数均摊且补发成本并进商品成本，抖音直接取每个子订单的
结算净额。指标上用 `by_platform` 写平台差异，对外仍是同一套损益项。

## 没进利润的钱

淘宝天猫的对账与推广优先使用平台原始订单明细：同一主订单的内部拆单、补发行不再
重复参与分摊。补发记录仍保留给成本与售后核算，不能承接原销售订单的收入和费用。

带日期列的推广明细按费用发生月份与商品 ID 同时匹配订单，跨月导出也不能把后月
费用分回前月。非零明细的日期缺失或无效时明确报错，不借订单月份入账；当月没有
对应商品订单的费用保留为未入账证据。没有日期列的商品汇总表继续使用既有汇总口径。

销售退款采用资金流水发生月份：6 月发生的退款记入 6 月，即使原订单属于 5 月；
7 月退款不能因关联到 6 月订单而进入 6 月。缺少订单关联的已归类退款按发生月单列，
原文件和行号继续留档；缺少发生日期时不猜月份。模型用 `posting_basis: transaction`
明确这一口径，普通订单口径保持 `posting_basis: order`。

补发成本按原销售订单月份确认。只有补发记录、缺少原销售订单资料时，成本保留为
未入账证据，不用补发日期猜原单月份；已结账月份继续保留原冻结结果。
聚水潭中文状态“取消／已取消”和订单台状态码 `Cancelled` 均不计商品成本、补发成本，
1688 将补发并入商品成本时同样排除。取消只排除该内部订单的成本记录，不清空原始
单价，不删除原表，也不连带排除同一平台订单号下仍有效的内部订单。
拼多多的销售和平台费用也排除仅有补发记录的目标单，退款仍按流水发生月确认。

成本下钻留档同时保存平台主订单号、内部订单号和 SKU。查询完整子单号时，会展开
同一平台主单的组件成本，包含没有平台子单号而以内部行号留档的组件；这只改变
查询范围，不重复增加成本。旧快照没有这些关联字段时保持原样，不用当前资料改写历史。

挂不上订单的钱一分都不会被悄悄丢掉，但也不是都要人去查。按「为什么挂不上」分四类，
只有最后一类需要行动：

| 分类 | 什么意思 |
| --- | --- |
| 其他店的数据（公司级主表） | 运费和小额打款交上来是全公司的，别家店的运单不算这家店的账 |
| 非经营流水（规则已排除） | 理财申购、银行间调拨、保证金进出、广告预充值，不是损益 |
| 其他账期的订单 | 订单号是对的但订单不在本期。跨期结算，或者导出时日期选宽了 |
| 取不出订单号，要查归属 | 这一类才要人查，查清了才会进利润 |

分开报是必须的。淘宝 5 月曾经报出一个 -30.29 万的「未归属」，而那个数字没有业务含义
——它是 -47.78 万非经营流水和 +17.52 万跨期订单收款相减的净额，真正要查的只有 5 行、
308.31 元。把它们加在一起摆出来，用户要么白查一场，要么学会无视这个数。

覆盖率同理：它的分母是「预期有这项数据的订单」，不是全部订单。没发货的订单不会有出库
成本，全额退款的订单没有净收款，把它们算进分母只会得到一个永远不达标的假缺口。指标上
用 `expect` 声明这件事。

## 店铺配置

法人主体、别名、归档状态都是配置项，不用手编 YAML。页面上有「店铺设置」，命令行是：

```bash
.venv/bin/python -m ledger.cli stores                                  # 看全部店铺和主体
.venv/bin/python -m ledger.cli store set <id> --entity 主体全名 --tax-id 税号
.venv/bin/python -m ledger.cli store set <id> --alias 旧店名            # 改过名的店
.venv/bin/python -m ledger.cli store set <id> --archive                # 关店，历史账仍可重算
.venv/bin/python -m ledger.cli store add pdd_xinbao 拼多多新店 --platform pdd
```

主体和店铺是多对一——1688星泽气球派对 和 抖音浅花涧节日装饰 同属一家主体。这层关系
推不出来只能配。有些平台的数据里带主体（1688 收款明细有「归属主体名称」、抖音对账单有
「商户主体名称」），淘宝那份读不到，支付宝和微信账单不带主体信息，引擎不猜。

写回会保住文件里的注释（那些注释记着每个值是从哪张表哪一列取证来的），写完立刻校验，
校验不过就回滚。`id` 和 `name` 不让改：`id` 是关联键，`name` 是认文件的依据，改了历史账
就对不上——店铺改名请加别名，把旧名留着。

交上来的文件如果认不出归属，CLI 和页面都会列出来，并且照着文件名给一条建议。平台前缀
能认出来的会一并填好——那只是建议，猜出来的东西不参与任何计算。

## 测试

```bash
.venv/bin/pip install -r requirements-dev.txt
.venv/bin/python -m pytest                    # 全部
.venv/bin/python -m pytest -m "not slow"      # 跳过要真实数据的
```

最有价值的是 `tests/test_acceptance.py`：它拿引擎算的账和人工维护的 Excel 逐项对，
断言三家店的未解释差异为零。它需要真实的平台导出（不在仓库里），没有就自动跳过。

其余测试各钉一个在真实数据上踩过的坑，docstring 里写明是哪一个。改动这些测试前
先读那段说明——它们防的都是不会报错、只会算错的问题。
